services:
  - type: web
    name: algonix
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
        sync: false
      - key: GLOT_API_TOKEN
        sync: false
      - key: PISTON_URL
        sync: false
      # Use Glot API for execution when possible. Piston is now whitelist-only.
      - key: FRONTEND_URL
        sync: false
