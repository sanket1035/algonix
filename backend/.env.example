PORT=5000
MONGODB_URI=mongodb://localhost:27017/algonix
JWT_SECRET=your_jwt_secret_key_here
PISTON_URL=https://emkc.org/api/v2/piston/execute
# Default Piston execution endpoint; replace with your self-hosted Piston URL if public service is blocked
# Piston is used for code execution; no external API key required
# JUDGE0_URL and RAPIDAPI_KEY are no longer needed
NODE_ENV=development
FRONTEND_URL=http://localhost:3000